/* eslint-disable no-undef */
// eslint-disable-next-line no-unused-vars
const assert = require('assert');

describe('CFD Home Page', () => {
  beforeEach(() => {
    browser.url('./');
  });

  it('should be able to sign in', () => {});
});
